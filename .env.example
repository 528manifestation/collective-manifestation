# Supabase client settings for Vite.
# Do not commit real secrets. The anon key is public by design, but keep project-specific values local until configured.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
